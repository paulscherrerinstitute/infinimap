# Raw ibdiagnet snapshot data + parsed
/current/
/out/
/test_data/
backend/ibcore/data/
split_db_csv.py

# Operator-supplied display-name overrides. Site data: real GUIDs and real
# hostnames. Ship only the example.
backend/infinimap/api/id_to_name.json

# Working notes and scratch analysis, not part of the published tool
/docs/
timing_analysis.ipynb
polltest.py

# Service configs - may hold DB credentials; we commit only the .example files
backend/collector/collector.toml
backend/api/api.toml
backend/infinimap/collector/collector.toml
backend/infinimap/api/api.toml
backend/infinimap/api/id_to_name.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Python build artefacts (pyproject.toml / python -m build / pip install -e)
build/
dist/
*.egg-info/

# Node / frontend build
frontend/node_modules/
frontend/dist/
# The built UI copied into the package before a wheel is built. A build
# artefact, never a source file.
backend/infinimap/api/web/
frontend/tsconfig.tsbuildinfo

# Editor / OS
.DS_Store
Thumbs.db


