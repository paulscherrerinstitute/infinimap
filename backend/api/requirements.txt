# psycopg 3 is the standard PostgreSQL driver for Python 3
psycopg[binary]>=3.1,<4

# Connection pooling. The collector holds one connection for its process
# lifetime; a server answering concurrent requests cannot.
psycopg-pool>=3.2,<4

fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1

# Only needed by `python -m api.selftest`, which drives the app through
# TestClient rather than over a socket.
httpx>=0.27,<1
